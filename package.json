{
    "name": "finance-tracker-root",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "build": "npm run build --workspace=client"
    },
    "dependencies": {
        "@prisma/client": "^5.10.0",
        "express": "^5.2.1"
    },
    "devDependencies": {
        "prisma": "5.10.0"
    }
}